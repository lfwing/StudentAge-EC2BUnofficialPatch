# 1.0.24-handoff.3

> 历史记录：本文件内容已合并到当前 `CHANGELOG_1.0.21.md`，这里只保留原始版本标注。

2026-09-13，本地接手开发版本。

- 音频监控路径改按 AudioClip 弱引用键关联，避免永久实例 ID 缓存。
- 音频日志改用有上限的当前/前帧集合；先去重再构造对象路径，调用栈省去额外数组副本。极端突发超过每帧512个缓存项时继续输出事件，仅放弃超量部分的去重。
- 更新版本的缺省 build/revision 按0比较，修复等价版本反复提示更新；手动下载提示跟随当前整合来源。
- 测试工程分离中间输出，修复并行检查时文件互相覆盖。
- 保持两布局、旧 GUID、公开 API、UP AssemblyVersion 1.0.9.0 和 BA 逻辑版本1.0.0；不更改模组资源及存档格式。

小游戏协作模块0.3.1另修正 UP 模式奖励说明，独立交付。当前验证见 ../../Docs/VALIDATION.md；待办沿用对应接手记录。
